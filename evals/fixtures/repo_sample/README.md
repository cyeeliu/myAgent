# Sample Repository

This is a small fixture repository for evaluation tasks.

## Structure

```
src/
  main.py    - entry point with foo() and bar() functions
  utils.py   - utility functions
README.md    - this file
```

## Functions

- `foo()` — returns "hello"
- `bar()` — returns "world"
- `baz(x)` — returns x * 2
